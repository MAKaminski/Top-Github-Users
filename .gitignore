node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

.env*
!.env.example

test-results/
playwright-report/
.DS_Store

# Regenerated by `pnpm crawl --tier=discover` in seconds; not worth committing.
data/discovery/

# Regenerated by `node tests/launch-media.mjs` from the running site. Committing
# the PNGs would mean the repository carries stale screenshots of itself.
launch-media/
